<h2> This request has already been
  {{#if request.approved}}
    Approved
  {{else}}
    {{#compare @root.request.approver "System"}}
      Archived
    {{else}}
      Rejected
    {{/compare}}
  {{/if}}
</h2>
