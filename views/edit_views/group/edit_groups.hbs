{{#if request}}
{{#compare request "success"}}
    <div class="alert alert-success" role="alert">
        {{alert_msg}}
    </div>
{{/compare}}
{{#compare request "failure"}}
    <div class="alert alert-warning" role="alert">
        {{alert_msg}}
    </div>
{{/compare}}
{{/if}}

<div class="content">
    <h1> Groups </h1>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Email</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            {{#each groups}}
                    <tr>
                        <th scope="row">{{this.name}}</th>
                        <td>{{this.email}}</td>
                        <td><button type="button" class="btn btn-primary">Edit</button></a></td>
                    </tr>
            {{/each}}
        </tbody>
    </table>
</div>
